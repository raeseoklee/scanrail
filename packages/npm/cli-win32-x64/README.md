# @scanrail/cli-win32-x64

Platform binary package for `@scanrail/cli` on Windows x64.

Install `@scanrail/cli` instead of installing this package directly:

```bash
npm install -g @scanrail/cli
```

License: Apache-2.0
