# @scanrail/cli-win32-x64

Platform binary package for Scanrail on Windows x64.

Install `scanrail` instead of installing this package directly:

```bash
npm install -g scanrail
```

License: Apache-2.0
