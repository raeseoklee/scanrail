# @scanrail/cli-linux-arm64

Platform binary package for Scanrail on Linux arm64.

Install `scanrail` instead of installing this package directly:

```bash
npm install -g scanrail
```

License: Apache-2.0
